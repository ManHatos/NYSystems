{
	"name": "ny-systems",
	"version": "0.0.0-canary.1",
	"author": "ManHat",
	"type": "module",
	"main": "index.js",
	"scripts": {
		"p-gen": "npx prisma generate",
		"p-sync": "npx prisma db push",
		"clean": "rimraf dist/ -G dist",
		"build": "npm run p-gen && npm run clean && npx tsc",
		"start": "npm run build && node dist/",
		"test": "npm run build && node dist/play.local.js",
		"bm": "npm run build && node dist/benchmark.local.js"
	},
	"dependencies": {
		"@discordeno/bot": "^19.0.0-next.7aed376",
		"@prisma/client": "^5.6.0",
		"chalk": "^5.3.0",
		"dotenv": "^16.3.1",
		"dotenv-vault": "^1.25.0",
		"redis": "^4.6.10"
	},
	"devDependencies": {
		"@types/node": "^20.9.2",
		"prisma": "^5.6.0",
		"rimraf": "^5.0.5",
		"typescript": "^5.2.2"
	}
}
