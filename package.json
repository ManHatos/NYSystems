{
	"name": "ny-systems",
	"version": "0.0.0-canary.3.0.8",
	"author": "ManHat",
	"type": "module",
	"main": "index.js",
	"scripts": {
		"test": "npm run build && node dist/play.local.js",
		"bm": "npm run build && node dist/benchmark.local.js",
		"p-gen": "npx prisma generate",
		"p-sync": "npx prisma db push",
		"clean": "rimraf dist/ -G dist",
		"build": "npm run p-gen && npm run clean && npx tsc",
		"start": "npm run build && node dist/"
	},
	"dependencies": {
		"@discordeno/bot": "^19.0.0-next.d81b28a",
		"@prisma/client": "^5.8.0",
		"chalk": "^5.3.0",
		"dotenv": "^16.3.1",
		"dotenv-vault": "^1.25.0",
		"redis": "^4.6.12"
	},
	"devDependencies": {
		"@types/node": "^20.11.0",
		"prisma": "^5.8.0",
		"rimraf": "^5.0.5",
		"typescript": "^5.3.3"
	}
}
